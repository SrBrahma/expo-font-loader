{
  "name": "expo-font-loader",
  "version": "0.1.5",
  "description": "Smartly load and use expo fonts and icons!",
  "main": "./lib/index.js",
  "scripts": {
    "test": "echo \"No test specified.\"",
    "lint": "eslint --fix \"src/**\"",
    "clean": "rimraf lib",
    "build": "npm run clean && tsc",
    "prepublishOnly": "npm run lint && npm run test",
    "prepare": "npm run build",
    "watch": "ts-node-dev --respawn --transpile-only ./src/index.ts",
    "start": "npm run watch"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "files": [
    "/lib"
  ],
  "dependencies": {
    "expo-font": "*"
  },
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "@types/node": "^17.0.15",
    "eslint-config-gev": "^2.27.1",
    "jest": "^27.5.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^27.1.3",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.5.5",
    "react-native": "*",
    "@types/react-native": "*"
  },
  "peerDependencies": {
    "react-native": "*"
  }
}